# Usage

Purpose: route local audio/video transcription to the appropriate configured backend.
Trigger: a local media file needs transcript, timestamps, subtitles, or speech-edit support.
Usage: select the backend from output requirements; never silently switch when the user explicitly fixed a provider.
