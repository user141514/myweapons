# 使用说明

用途：新增能力、纵向模块、系统迁移或跨边界故障时，从用户结果推导最小系统变更面。

触发：owner、consumer、repository、deployment dependency 或 acceptance 仍不清楚。

最小使用：复用 TARGET / REFERENCE / DELTA / FRONTIER / PROBE，并补 SURFACE；dependency closure 不等于 ownership closure；找到最小可实施面后交回 software-engineering-review。
